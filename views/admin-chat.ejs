<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
    <title>แชท - ChatCenter AI</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
    <link href="/css/chat-new.css" rel="stylesheet">
    <!-- ✅ New CSS Files -->
    <link href="/css/mobile-improvements.css" rel="stylesheet">
    <link href="/css/loading-states.css" rel="stylesheet">
    <link href="/css/error-handler.css" rel="stylesheet">
    <link href="/css/animations.css" rel="stylesheet">
</head>
<body>
    <!-- Navigation -->
    <%- include('partials/admin-navbar', { activePage: 'chat' }) %>

    <!-- Main Chat Interface -->
    <div class="chat-container">
        <!-- Sidebar Overlay for Mobile -->
        <div class="sidebar-overlay" id="sidebarOverlay"></div>
        
        <!-- User List Sidebar - Facebook Business Suite Style -->
        <div class="user-sidebar" id="userSidebar">
            <div class="sidebar-header">
                <div class="d-flex align-items-center justify-content-between w-100">
                    <div>
                        <h4 class="mb-0">
                            <i class="fas fa-comments me-2"></i>แชท
                        </h4>
                    </div>
                    <div class="d-flex align-items-center gap-2">
                        <div class="user-count" id="userCount">0</div>
                        <button class="btn btn-sm btn-link p-0 d-lg-none" id="toggleSidebar" title="ปิด" style="font-size: 1.25rem; color: var(--text-secondary);">
                            <i class="fas fa-times"></i>
                        </button>
                    </div>
                </div>
            </div>
            <!-- Search & Filter -->
            <div class="sidebar-search">
                <div class="input-group">
                    <span class="input-group-text">
                        <i class="fas fa-search"></i>
                    </span>
                    <input type="text" class="form-control" id="userSearch" placeholder="ค้นหาผู้ใช้...">
                    <button class="btn" type="button" id="filterToggle" title="ตัวกรอง">
                        <i class="fas fa-filter"></i>
                        <span class="filter-active-badge" id="filterActiveBadge" style="display: none;"></span>
                    </button>
                </div>
                <!-- Filter Options (Hidden by default) -->
                <div class="filter-options" id="filterOptions" style="display: none;">
                    <div class="d-flex justify-content-between align-items-center mb-2">
                        <small class="text-muted fw-bold">สถานะ</small>
                        <button class="btn btn-sm btn-link p-0 text-decoration-none" id="clearFilters">
                            <small>ล้างทั้งหมด</small>
                        </button>
                    </div>
                    <div class="d-flex gap-2 flex-wrap mb-3">
                        <button class="filter-btn active" data-filter="all">
                            <i class="fas fa-list"></i> ทั้งหมด
                        </button>
                        <button class="filter-btn" data-filter="unread">
                            <i class="fas fa-circle" style="font-size: 0.5rem;"></i> ยังไม่อ่าน
                        </button>
                        <button class="filter-btn" data-filter="followup">
                            <i class="fas fa-star"></i> ติดตาม
                        </button>
                        <button class="filter-btn" data-filter="purchased">
                            <i class="fas fa-shopping-cart"></i> เคยซื้อแล้ว
                        </button>
                    </div>
                    <div class="mb-2">
                        <small class="text-muted fw-bold">กรองตามแท็ก</small>
                    </div>
                    <div id="tagFilters" class="d-flex gap-2 flex-wrap">
                        <span class="text-muted small">ไม่มีแท็กในระบบ</span>
                    </div>
                    <div class="mt-2 pt-2 border-top">
                        <small class="text-muted">
                            <span id="filteredUserCount">0</span> ผู้ใช้
                        </small>
                    </div>
                </div>
            </div>
            <div class="user-list" id="userList">
                <div class="loading-state">
                    <i class="fas fa-spinner fa-spin"></i>
                    <span>กำลังโหลด...</span>
                </div>
            </div>
        </div>

        <!-- Chat Area -->
        <div class="chat-main">
            <!-- Chat Header -->
            <div class="chat-header" id="chatHeader">
                <div class="header-content">
                    <div class="user-info">
                        <div class="user-avatar">
                            <i class="fas fa-user"></i>
                        </div>
                        <div class="user-details">
                            <h6>เลือกผู้ใช้เพื่อเริ่มการสนทนา</h6>
                            <small>0 ข้อความ</small>
                        </div>
                    </div>
                    <div class="header-actions" id="headerActions" style="display: none;">
                        <button class="btn btn-sm btn-outline-danger" onclick="clearUserChat()">
                            <i class="fas fa-trash me-1"></i>ล้าง
                        </button>
                    </div>
                </div>
            </div>

            <!-- Messages Area -->
            <div class="messages-container" id="messagesContainer">
                <div class="empty-state">
                    <div class="empty-icon">
                        <i class="fas fa-comments"></i>
                    </div>
                    <h5>ยินดีต้อนรับสู่ระบบแชท ChatCenter AI</h5>
                    <p>เลือกผู้ใช้จากรายชื่อด้านซ้ายเพื่อเริ่มการสนทนา หรือค้นหาผู้ใช้ที่ต้องการติดต่อ</p>
                </div>
            </div>

            <!-- Message Input -->
            <div class="message-input-container" id="messageInputContainer" style="display: none;">
                <div class="input-group">
                    <textarea class="form-control" id="messageInput" 
                              placeholder="พิมพ์ข้อความ... (Shift+Enter = บรรทัดใหม่)" 
                              maxlength="1000" autocomplete="off" rows="1"></textarea>
                    <button class="btn btn-primary" type="button" id="sendButton">
                        <i class="fas fa-paper-plane"></i>
                    </button>
                </div>
                <div class="input-footer">
                    <small>
                        <span id="charCount">0</span>/1000
                    </small>
                    <small>
                        Enter = ส่ง • Shift+Enter = บรรทัดใหม่
                    </small>
                </div>
            </div>
        </div>
        
        <!-- Toggle Sidebar Button (Mobile) -->
        <button class="sidebar-toggle-btn" id="sidebarToggleBtn">
            <i class="fas fa-users"></i>
        </button>
    </div>

    <!-- Image Modal -->
    <div class="modal fade" id="imageModal" tabindex="-1">
        <div class="modal-dialog modal-lg modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">รูปภาพ</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body text-center">
                    <img id="modalImage" src="" alt="รูปภาพ" class="img-fluid">
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-outline-primary" id="downloadImage">
                        <i class="fas fa-download me-1"></i>ดาวน์โหลด
                    </button>
                    <button type="button" class="btn btn-outline-secondary" id="copyImage">
                        <i class="fas fa-copy me-1"></i>คัดลอก
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Loading Modal -->
    <div class="modal fade" id="loadingModal" tabindex="-1">
        <div class="modal-dialog modal-sm modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-body text-center">
                    <i class="fas fa-spinner fa-spin fa-2x text-primary mb-2"></i>
                    <p class="mb-0">กำลังโหลด...</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Tag Management Modal -->
    <div class="modal fade" id="tagModal" tabindex="-1">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">
                        <i class="fas fa-tags me-2"></i>จัดการแท็ก
                    </h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <div class="mb-3">
                        <label class="form-label fw-bold">ผู้ใช้:</label>
                        <div id="tagModalUserName" class="text-muted"></div>
                    </div>

                    <div class="mb-3">
                        <label class="form-label fw-bold">แท็กปัจจุบัน:</label>
                        <div id="currentTags" class="d-flex flex-wrap gap-2 mb-2">
                            <span class="text-muted small">ยังไม่มีแท็ก</span>
                        </div>
                    </div>

                    <div class="mb-3">
                        <label class="form-label fw-bold">เพิ่มแท็กใหม่:</label>
                        <div class="input-group">
                            <input type="text" class="form-control" id="newTagInput" 
                                   placeholder="พิมพ์ชื่อแท็ก..." maxlength="50">
                            <button class="btn btn-primary" type="button" id="addTagBtn">
                                <i class="fas fa-plus me-1"></i>เพิ่ม
                            </button>
                        </div>
                    </div>

                    <div class="mb-3">
                        <label class="form-label fw-bold">แท็กที่ใช้บ่อย:</label>
                        <div id="popularTags" class="d-flex flex-wrap gap-2">
                            <span class="text-muted small">กำลังโหลด...</span>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">ปิด</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Toast Container -->
    <div class="toast-container position-fixed bottom-0 end-0 p-3">
        <div id="toast" class="toast" role="alert">
            <div class="toast-header">
                <i class="fas fa-info-circle text-primary me-2"></i>
                <strong class="me-auto">แจ้งเตือน</strong>
                <button type="button" class="btn-close" data-bs-dismiss="toast"></button>
            </div>
            <div class="toast-body" id="toastBody"></div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        window.chatCenterFollowUpConfig = {
            analysisEnabled: <%= (followUpConfig && followUpConfig.analysisEnabled) ? 'true' : 'false' %>,
            showInChat: <%= (followUpConfig && followUpConfig.showInChat) ? 'true' : 'false' %>
        };
    </script>
    <script src="/socket.io/socket.io.js"></script>
    <!-- ✅ New JS Files - Load before chat-new.js -->
    <script src="/js/performance-utils.js"></script>
    <script src="/js/loading-states.js"></script>
    <script src="/js/error-handler.js"></script>
    <script src="/js/chat-new.js"></script>
</body>
</html>
