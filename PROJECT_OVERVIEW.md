# เอกสารสรุปโครงสร้างโปรเจกต์ ChatCenterAI-4

เอกสารฉบับนี้สรุปภาพรวมทางเทคนิค โครงสร้างไฟล์ และการทำงานของระบบ ChatCenterAI-4 ซึ่งเป็นเว็บแอปพลิเคชันสำหรับจัดการแชทบอทและระบบตอบกลับอัตโนมัติ

## 1. ภาพรวม (Overview)
ChatCenterAI-4 เป็นแอปพลิเคชันแบบ **Monolithic** ที่พัฒนาด้วย **Node.js** และ **Express.js** ทำหน้าที่เป็นทั้ง Web Server และ API Server โดยมีการแสดงผลฝั่งหน้าบ้าน (Frontend) ด้วย **EJS (Server-Side Rendering)** และใช้ **MongoDB** เป็นฐานข้อมูลหลัก ระบบรองรับการเชื่อมต่อกับ LINE OA และ OpenAI เพื่อทำฟังก์ชัน AI Chatbot

## 2. เทคโนโลยีที่ใช้ (Tech Stack)

*   **Core Runtime:** Node.js
*   **Web Framework:** Express.js
*   **Database:** MongoDB (Native Driver & Connect-Mongo)
*   **Templating Engine:** EJS (Embedded JavaScript)
*   **Authentication:** Custom Passcode System (bcryptjs + express-session)
*   **Real-time Communication:** Socket.io
*   **External APIs:**
    *   LINE Messaging API (`@line/bot-sdk`)
    *   OpenAI API (`openai`)
    *   Google APIs (`googleapis`)
*   **Utilities:** Axios, Sharp (Image Processing), Moment-timezone

## 3. โครงสร้างไดเรกทอรี (Directory Structure)

### 📂 Root Directory
ไฟล์หลักสำหรับการตั้งค่าและการรันระบบ
*   `index.js`: จุดเริ่มต้นของโปรแกรม (Entry Point) รวมการตั้งค่า Server, Route, DB connection และ Socket.io
*   `config.js`: ไฟล์รวบรวมค่า Configuration ต่างๆ
*   `.env`: ไฟล์เก็บค่า Environment Variables (Secrets)
*   `Dockerfile` & `railway.json`: การตั้งค่าสำหรับการ Deploy บน Container Platform (รองรับ Railway)

### 📂 /utils
ฟังก์ชันอรรถประโยชน์และ Logic ที่ถูกแยกออกมา
*   `auth.js`: ระบบยืนยันตัวตน (Authentication)
    *   จัดการ Passcode (Create, Verify, Delete)
    *   ตรวจสอบสิทธิ์ Admin/Superadmin
    *   เข้ารหัสรหัสผ่านด้วย `bcryptjs`

### 📂 /scripts
สคริปต์สำหรับการดูแลรักษาและจัดการข้อมูล (Maintenance & Operations)
*   `mongo-backup.sh`: Shell script สำหรับ Backup ฐานข้อมูล MongoDB พร้อมระบบหมุนเวียนไฟล์ (Retention Policy)
*   `migrate-to-instructions-v2.js`: สคริปต์สำหรับย้ายข้อมูล (Migration) จากโครงสร้าง Instruction แบบเก่าไปสู่เวอร์ชันใหม่ (`instructions_v2`)
*   `fix-asset-consistency.js`: ตรวจสอบและแก้ไขความสมบูรณ์ของไฟล์ Assets

### 📂 /views (Frontend Templates)
ไฟล์ Template สำหรับการแสดงผล (UI) แบ่งตามการใช้งาน
*   **Dashboards:**
    *   `admin-dashboard-v2.ejs`: หน้าหลักสำหรับจัดการ AI Instructions (เวอร์ชันใหม่)
    *   `admin-orders.ejs`: หน้าจัดการรายการสั่งซื้อ
    *   `admin-chat-modern.ejs`: หน้าจอแชทสำหรับแอดมิน
*   **System Settings:** อยู่ในโฟลเดอร์ `partials/settings/` เช่น `bot-ai.ejs`, `security.ejs`
*   **Components:** อยู่ในโฟลเดอร์ `partials/` เช่น Navbar (`admin-navbar.ejs`) และ Modals ต่างๆ

### 📂 /public (Static Assets)
ไฟล์ที่ทำงานฝั่ง Client Browser
*   **JS:**
    *   `admin-dashboard-v2.js`: Logic ควบคุมหน้า Dashboard, การจัดการ Form, และ AJAX Calls
    *   `chat-new.js`: Logic การทำงานของห้องแชท
*   **CSS:** ไฟล์สไตล์ชีทแยกตามหน้างาน เช่น `admin-settings.css`, `chat-redesign.css`
*   **Assets:** เก็บไฟล์รูปภาพหรือไฟล์แนบใน `assets/`

## 4. ระบบสำคัญ (Key Systems)

### ระบบ Authentication (Passcode)
ระบบใช้การยืนยันตัวตนผ่าน "Passcode" แทน Username/Password แบบปกติ
*   มีการแยก Role (Admin / Superadmin) ผ่าน `masterPasscode`
*   เก็บ Log การใช้งาน Passcode (Last Used, IP Address)

### ระบบจัดการ AI Instructions (V2)
*   พัฒนาต่อยอดให้ 1 Instruction สามารถมี Data Items ย่อยได้หลายรายการ
*   มีหน้า UI (`admin-dashboard-v2.ejs`) ที่รองรับการเพิ่ม/ลบ/แก้ไข Instruction แบบ Inline
*   เก็บข้อมูลใน Collection `instructions_v2`

## 5. การติดตั้งและการรัน (Deployment)
*   **Start Server:** `npm start` (รัน `node index.js`)
*   **Dev Mode:** `npm run dev` (รันด้วย `nodemon`)
*   **Environment:** จำเป็นต้องมีไฟล์ `.env` ที่ระบุค่า `MONGO_URI`, `LINE_CHANNEL_ACCESS_TOKEN`, `OPENAI_API_KEY` และอื่นๆ ให้ครบถ้วน

---
*เอกสารฉบับนี้ถูกสร้างขึ้นอัตโนมัติจากการวิเคราะห์โครงสร้างโปรเจกต์ ณ วันที่ 19 พฤศจิกายน 2025*
