# Frontend Bugs & UX Issues Report
## ChatCenterAI Dashboard & Instruction Editor

**วันที่ตรวจสอบ:** 13 ธันวาคม 2567  
**ผู้ตรวจสอบ:** AI Code Review  
**ขอบเขต:** Dashboard (admin-dashboard-v2) และหน้าแก้ไข Instruction (แบบข้อความ/ตาราง)

---

## 📋 สารบัญ

1. [สรุปปัญหาเร่งด่วน (Critical)](#1-สรุปปัญหาเร่งด่วน-critical)
2. [ปัญหาสำคัญ (Major)](#2-ปัญหาสำคัญ-major)
3. [ปัญหาทั่วไป (Minor)](#3-ปัญหาทั่วไป-minor)
4. [ข้อเสนอแนะด้าน UX](#4-ข้อเสนอแนะด้าน-ux)

---

## 1. สรุปปัญหาเร่งด่วน (Critical)

### 🔴 C-001: ข้อมูลไม่ซิงค์เมื่อสลับ Instruction
**หน้า:** Dashboard (admin-dashboard-v2)  
**รายละเอียด:** เมื่อผู้ใช้มีการแก้ไขที่ยังไม่บันทึก (`isDirty=true`) และยกเลิกการเปลี่ยน Instruction ผ่าน confirm dialog ค่าใน dropdown จะถูก set กลับ แต่ UI state อาจไม่ sync อย่างถูกต้อง

**ไฟล์ที่เกี่ยวข้อง:**
- `public/js/admin-dashboard-v2.js` (lines 251-261)

**Code snippet:**
```javascript
instructionSelect.addEventListener('change', () => {
    const selectedId = instructionSelect.value;
    if (editorState.isDirty && selectedId !== editorState.currentInstructionId &&
        !confirm('มีการแก้ไขที่ยังไม่บันทึก ต้องการละทิ้งการเปลี่ยนแปลงหรือไม่?')) {
        instructionSelect.value = editorState.currentInstructionId || ''; // reset but UI may desync
        return;
    }
    loadInstructionIntoEditor(selectedId);
});
```

**ผลกระทบ:** ข้อมูลอาจสูญหายโดยไม่ตั้งใจ  
**ความเร่งด่วน:** สูง

---

### 🔴 C-002: Race Condition ในการโหลด Instruction
**หน้า:** Dashboard (admin-dashboard-v2)  
**รายละเอียด:** ใช้ `editorRequestToken` เพื่อป้องกัน race condition แต่หากผู้ใช้คลิกเลือก Instruction เร็วมากหลายครั้งติดต่อกัน request ก่อนหน้าอาจยังทำงานอยู่และทำให้ loading state สับสน

**ไฟล์ที่เกี่ยวข้อง:**
- `public/js/admin-dashboard-v2.js` (lines 190-248)

**ผลกระทบ:** UI อาจแสดงข้อมูลไม่ตรงกับ Instruction ที่เลือก  
**ความเร่งด่วน:** สูง

---

### 🔴 C-003: Export Excel ไม่ใช่รูปแบบ XLSX จริง
**หน้า:** Edit Data Item V3 (edit-data-item-v3.ejs)  
**รายละเอียด:** ฟังก์ชัน `exportAsExcel()` สร้างไฟล์ที่เป็น TSV แต่ตั้งนามสกุลเป็น `.xlsx` ซึ่งไม่ใช่ไฟล์ Excel ที่ถูกต้อง บางโปรแกรมอาจเปิดไม่ได้หรือแจ้งว่าไฟล์เสียหาย

**ไฟล์ที่เกี่ยวข้อง:**
- `views/edit-data-item-v3.ejs` (lines 1210-1240)

**Code snippet:**
```javascript
// Download as .xlsx (Excel can open TSV)
// For true xlsx, we'd need a library like SheetJS
var blob = new Blob([csvContent], { type: 'application/vnd.ms-excel;charset=utf-8;' });
link.download = filename + '.xlsx'; // ❌ Not real xlsx format
```

**แนวทางแก้ไข:** ใช้ library เช่น SheetJS หรือส่ง request ไป backend เพื่อสร้างไฟล์ Excel จริง  
**ความเร่งด่วน:** สูง

---

## 2. ปัญหาสำคัญ (Major)

### 🟠 M-001: ไม่รองรับ Mobile อย่างเหมาะสมในหน้าแก้ไขตาราง
**หน้า:** Edit Data Item V2 & V3  
**รายละเอียด:** Table builder มี `min-width: 780px` ทำให้บน mobile ต้อง scroll แนวนอนมาก และ textarea ในเซลล์ก็ยากต่อการพิมพ์บน mobile

**ไฟล์ที่เกี่ยวข้อง:**
- `views/edit-data-item-v2.ejs` (lines 46-56)

**Code snippet:**
```css
.table-builder-wrapper table {
    min-width: 780px; /* ยากต่อการใช้งานบน mobile */
}
```

**ผลกระทบ:** ประสบการณ์ใช้งานแย่บน mobile และ tablet  
**ความเร่งด่วน:** ปานกลาง

---

### 🟠 M-002: Column/Row Resize Handle มีขนาดเล็กเกินไป
**หน้า:** Edit Data Item V2  
**รายละเอียด:** `.column-resizer` และ `.row-resizer` มีขนาดเล็ก (12px และ 8px) ทำให้ลากปรับขนาดได้ยาก โดยเฉพาะบน touch screen

**ไฟล์ที่เกี่ยวข้อง:**
- `views/edit-data-item-v2.ejs` (lines 279-264)

**Code snippet:**
```css
.column-resizer {
    width: 12px; /* เล็กเกินไป */
}
.row-resizer {
    height: 8px; /* เล็กเกินไป */
}
```

**แนวทางแก้ไข:** เพิ่มขนาดเป็น 16-20px และใช้ transparent hitbox ที่ใหญ่กว่า  
**ผลกระทบ:** ความยากลำบากในการปรับขนาด  
**ความเร่งด่วน:** ปานกลาง

---

### 🟠 M-003: Cell Flyout อาจหลุดนอกจอ
**หน้า:** Edit Data Item V2  
**รายละเอียด:** `showCellFlyout()` พยายามวางตำแหน่ง flyout แต่การคำนวณอาจทำให้ flyout หลุดไปนอกขอบจอด้านล่าง

**ไฟล์ที่เกี่ยวข้อง:**
- `views/edit-data-item-v2.ejs` (lines 1812-1848)

**Code snippet:**
```javascript
flyout.wrapper.style.top = `${rect.top + scrollTop}px`;
// ❌ ไม่ได้ตรวจสอบว่าจะหลุดไปด้านล่างของ viewport หรือไม่
```

**แนวทางแก้ไข:** ตรวจสอบ viewport boundaries และปรับตำแหน่งถ้าจำเป็น  
**ผลกระทบ:** ผู้ใช้มองไม่เห็น flyout หรือต้อง scroll  
**ความเร่งด่วน:** ปานกลาง

---

### 🟠 M-004: Preview Instruction ไม่ Auto-refresh หลังแก้ไข
**หน้า:** Dashboard (admin-dashboard-v2)  
**รายละเอียด:** เมื่อบันทึกการแก้ไขชื่อ/คำอธิบาย Instruction ผ่าน inline editor การ preview ยังคงแสดงข้อมูลเก่าจนกว่าจะ refresh หน้า

**ไฟล์ที่เกี่ยวข้อง:**
- `public/js/admin-dashboard-v2.js` (lines 478-500)

**ผลกระทบ:** ผู้ใช้อาจสับสนว่าข้อมูลบันทึกแล้วหรือยัง  
**ความเร่งด่วน:** ปานกลาง

---

### 🟠 M-005: Duplicate ByeName Conflict Warning หายไป
**หน้า:** Dashboard modal สำหรับ duplicate instruction  
**รายละเอียด:** ใช้ `prompt()` เพื่อขอชื่อ Instruction ใหม่ โดยไม่มีการตรวจสอบว่าชื่อซ้ำกับที่มีอยู่หรือไม่

**ไฟล์ที่เกี่ยวข้อง:**
- `public/js/admin-dashboard-v2.js` (lines 448-476)

**Code snippet:**
```javascript
const name = prompt('ชื่อ Instruction ใหม่:');
if (!name) return;
// ❌ ไม่มีการตรวจสอบชื่อซ้ำ
```

**ผลกระทบ:** อาจมี Instruction ที่ชื่อเหมือนกันหลายอัน ทำให้สับสน  
**ความเร่งด่วน:** ปานกลาง

---

### 🟠 M-006: beforeunload Warning ไม่ทำงานใน V3 Editor
**หน้า:** Edit Data Item V3  
**รายละเอียด:** Cancel button มีการ disable `window.onbeforeunload` ก่อน navigate ซึ่งถูกต้อง แต่ถ้าผู้ใช้กดปิดแท็บหรือปุ่ม back ของ browser ขณะที่ยังไม่ได้ confirm จะไม่มี warning

**ไฟล์ที่เกี่ยวข้อง:**
- `views/edit-data-item-v3.ejs` (lines 1257-1263)

**Code snippet:**
```javascript
window.onbeforeunload = function (e) {
    if (dirty) {
        e.preventDefault();
        return ''; // Works but empty string may not show custom message in all browsers
    }
};
```

**ผลกระทบ:** ข้อมูลอาจสูญหายโดยไม่ตั้งใจ  
**ความเร่งด่วน:** ปานกลาง

---

### 🟠 M-007: jspreadsheet Initialization Failure ไม่มี Retry
**หน้า:** Edit Data Item V3  
**รายละเอียด:** ถ้า jspreadsheet library โหลดไม่สำเร็จจะแสดง error message แต่ไม่มีปุ่ม retry หรือ fallback UI

**ไฟล์ที่เกี่ยวข้อง:**
- `views/edit-data-item-v3.ejs` (lines 872-877)

**Code snippet:**
```javascript
if (typeof jspreadsheet === 'undefined') {
    container.innerHTML = '<div class="p-3 text-danger">ไม่สามารถโหลดตัวแก้ไขตารางได้...</div>';
    return; // ❌ ไม่มี retry button
}
```

**แนวทางแก้ไข:** เพิ่มปุ่ม "ลองใหม่" เพื่อ reload หน้า  
**ผลกระทบ:** ผู้ใช้ต้อง refresh หน้าเอง  
**ความเร่งด่วน:** ปานกลาง

---

## 3. ปัญหาทั่วไป (Minor)

### 🟡 m-001: Escape HTML ซ้ำซ้อนใน edit-instruction.ejs
**หน้า:** Edit Instruction (edit-instruction.ejs)  
**รายละเอียด:** มีการ escape HTML ผ่าน EJS และ JavaScript ซ้ำกัน อาจทำให้ special characters แสดงผิดปกติ

**ไฟล์ที่เกี่ยวข้อง:**
- `views/edit-instruction.ejs` (lines 206-214)

---

### 🟡 m-002: Toast Container อาจถูกสร้างหลายครั้ง
**หน้า:** Dashboard  
**รายละเอียด:** `ensureToastContainer()` ตรวจสอบ `toastContainer` variable แต่ถ้า DOM element ถูกลบไปแล้ว variable ยังคงอ้างอิงค่าเก่าอยู่

**ไฟล์ที่เกี่ยวข้อง:**
- `public/js/admin-dashboard-v2.js` (lines 7-14)

**Code snippet:**
```javascript
const ensureToastContainer = () => {
    if (!toastContainer) { // ❌ ควรตรวจสอบว่า element ยัง exist ใน DOM หรือไม่
        toastContainer = document.createElement('div');
        // ...
    }
    return toastContainer;
};
```

---

### 🟡 m-003: Import File Token ไม่มีการ Expire
**หน้า:** Import Modal  
**รายละเอียด:** `importFileToken` ถูกเก็บไว้จนกว่าจะ reset แต่ถ้าผู้ใช้เปิด modal ทิ้งไว้นาน token อาจหมดอายุที่ server

**ไฟล์ที่เกี่ยวข้อง:**
- `public/js/import-export-manager.js` (lines 103-126)

---

### 🟡 m-004: Keyboard Shortcuts ไม่ทำงานเมื่อ Focus อยู่นอก Table
**หน้า:** Edit Data Item V2  
**รายละเอียด:** Ctrl+Z/Ctrl+Y สำหรับ undo/redo ทำงานเฉพาะเมื่อ focus อยู่ใน table cell แต่ไม่ทำงานเมื่อ focus อยู่ที่ title input หรือ body

**ไฟล์ที่เกี่ยวข้อง:**
- `views/edit-data-item-v2.ejs` (lines 1210-1227)

---

### 🟡 m-005: Data Item Cards ไม่มี Loading State
**หน้า:** Dashboard  
**รายละเอียด:** เมื่อ reorder data items ปุ่มจะถูก disabled แต่ไม่มี visual loading indicator บน cards

**ไฟล์ที่เกี่ยวข้อง:**
- `public/js/admin-dashboard-v2.js` (lines 657-688)

---

### 🟡 m-006: Confirm Dialog ใช้ Native Alert
**หน้า:** หลายหน้า  
**รายละเอียด:** ใช้ `confirm()` และ `alert()` ซึ่งเป็น native browser dialog ที่ดูไม่ทันสมัยและ block JavaScript execution

**ไฟล์ที่เกี่ยวข้อง:**
- `public/js/admin-dashboard-v2.js`
- `public/js/import-export-manager.js`
- `views/edit-data-item-v2.ejs`

---

### 🟡 m-007: Inconsistent Date Formatting
**หน้า:** Dashboard  
**รายละเอียด:** มีการ format วันที่หลายรูปแบบ: บางที่ใช้ `toLocaleString('th-TH')` บางที่ใช้ `toLocaleDateString('th-TH')`

**ไฟล์ที่เกี่ยวข้อง:**
- `views/admin-dashboard-v2.ejs` (lines 64-66, 113-118, 263-269, 329-332)

---

### 🟡 m-008: Missing Aria Labels สำหรับ Accessibility
**หน้า:** ทุกหน้า  
**รายละเอียด:** ปุ่ม icon-only หลายปุ่มไม่มี `aria-label` ที่เหมาะสม

**ไฟล์ที่เกี่ยวข้อง:**
- `views/admin-dashboard-v2.ejs` (lines 214-231)

**Code snippet:**
```html
<button class="btn btn-sm btn-outline-primary edit-instruction" data-id="...">
    <i class="fas fa-edit"></i>
    <!-- ❌ Missing aria-label -->
</button>
```

---

### 🟡 m-009: CSS Variable Fallback ไม่ครบ
**หน้า:** Edit Data Item V3  
**รายละเอียด:** บาง CSS ใช้ `var()` โดยมี fallback ไม่ครบ อาจทำให้สีผิดปกติถ้า variable ไม่ถูก define

**ไฟล์ที่เกี่ยวข้อง:**
- `views/edit-data-item-v3.ejs` (lines 135-147)

**Code snippet:**
```css
.save-status.dirty {
    background: rgba(var(--warning-rgb, 223, 169, 75), 0.15);
    /* ✓ มี fallback */
}
```

---

### 🟡 m-010: Console.log Statements ใน Production Code
**หน้า:** Edit Data Item V3  
**รายละเอียด:** มี `console.log` statements ที่ไม่ควรมีใน production

**ไฟล์ที่เกี่ยวข้อง:**
- `views/edit-data-item-v3.ejs` (lines 837-839)

**Code snippet:**
```javascript
console.log('=== V3 Spreadsheet Editor ===');
console.log('Server data:', serverData);
```

---

## 4. ข้อเสนอแนะด้าน UX

### 💡 UX-001: เพิ่ม Auto-save
**คำอธิบาย:** เพิ่มระบบ auto-save ทุก 30-60 วินาที เพื่อลดความเสี่ยงที่ข้อมูลจะสูญหาย

---

### 💡 UX-002: Drag & Drop สำหรับ Reorder
**คำอธิบาย:** เปลี่ยนจากปุ่ม up/down เป็น drag & drop สำหรับการจัดเรียง data items

---

### 💡 UX-003: Bulk Actions
**คำอธิบาย:** เพิ่มความสามารถในการเลือกหลาย items และดำเนินการพร้อมกัน (ลบ, duplicate, move)

---

### 💡 UX-004: Keyboard Navigation ที่ดีขึ้น
**คำอธิบาย:** เพิ่ม keyboard shortcuts สำหรับ:
- `Ctrl+N`: สร้าง Instruction ใหม่
- `Ctrl+D`: Duplicate
- `Delete`: ลบ (พร้อม confirm)

---

### 💡 UX-005: Search & Filter
**คำอธิบาย:** มี search box อยู่แล้วแต่มี filter อื่นๆ เพิ่มเติมเช่น filter by type (text/table), filter by last updated

---

### 💡 UX-006: Preview แบบ inline
**คำอธิบาย:** แสดง preview โดยไม่ต้องเปิด modal แยก อาจเป็น expandable card section

---

### 💡 UX-007: Breadcrumb Navigation
**คำอธิบาย:** เพิ่ม breadcrumb ในหน้า edit (Dashboard > Instruction Name > Data Item Name)

---

### 💡 UX-008: Empty State ที่ดีกว่า
**คำอธิบาย:** Empty states ปัจจุบันดี แต่อาจเพิ่ม onboarding tips หรือ quick actions

---

### 💡 UX-009: Loading Skeleton
**คำอธิบาย:** ใช้ skeleton loading แทน spinner เพื่อให้ผู้ใช้รู้ว่า layout จะเป็นอย่างไร

---

### 💡 UX-010: Error Recovery
**คำอธิบาย:** เมื่อเกิด error ควรมีปุ่ม "ลองใหม่" ไม่ใช่แค่แสดง error message

---

## 📊 สรุปสถิติ

| ประเภท | จำนวน | คำอธิบาย |
|--------|-------|----------|
| 🔴 Critical | 3 | ปัญหาที่ส่งผลกระทบร้ายแรง ควรแก้ไขเร่งด่วน |
| 🟠 Major | 7 | ปัญหาสำคัญที่ส่งผลต่อ UX ควรแก้ไขเร็ว |
| 🟡 Minor | 10 | ปัญหาเล็กน้อย สามารถแก้ไขภายหลัง |
| 💡 UX Suggestions | 10 | ข้อเสนอแนะเพื่อปรับปรุง UX |
| **รวม** | **30** | |

---

## 📝 ลำดับความสำคัญในการแก้ไข

### Phase 1 (เร่งด่วน - ภายใน 1 สัปดาห์)
1. C-001: ข้อมูลไม่ซิงค์เมื่อสลับ Instruction
2. C-002: Race Condition ในการโหลด Instruction
3. C-003: Export Excel ไม่ใช่รูปแบบ XLSX จริง

### Phase 2 (สำคัญ - ภายใน 2-3 สัปดาห์)
1. M-001: ไม่รองรับ Mobile
2. M-003: Cell Flyout หลุดนอกจอ
3. M-006: beforeunload Warning
4. M-007: jspreadsheet Retry Button

### Phase 3 (ปรับปรุง - ภายใน 1 เดือน)
- แก้ไขปัญหา Minor ทั้งหมด
- เพิ่ม UX improvements ตามข้อเสนอแนะ

---

*รายงานนี้สร้างโดยระบบตรวจสอบโค้ดอัตโนมัติ กรุณาตรวจสอบและยืนยันปัญหาแต่ละข้อก่อนดำเนินการแก้ไข*
